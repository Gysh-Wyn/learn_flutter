import 'package:flutter/material.dart';
import 'package:godisha_assignment_1/app_widget.dart';

/// The main entry point of the application.
///
/// This function is responsible for running the Flutter application by calling
/// the `runApp` method with the root widget of the app, which in this case is `MyApp`.
void main() {
  runApp(const MyApp());
}